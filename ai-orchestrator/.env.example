# FastAPI Application Configuration
APP_ENV=local
APP_DEBUG=true

# OpenAI API Key (重要：請務必填寫您的 OpenAI API Key)
# 如果您在專案根目錄的 .env 中已設定 OPENAI_API_KEY，且所有服務共用該變數，
# 則此處可留空，但建議為每個服務獨立配置以確保清晰性。
OPENAI_API_KEY=your_openai_api_key_here

# Faster-Whisper Model Configuration
# 可選的模型大小：tiny, base, small, medium, large-v2, large-v3
# 較大的模型提供更好的準確度，但也需要更多的記憶體和處理能力。
WHISPER_MODEL=tiny

# 運行設備：cpu (CPU), cuda (GPU)
# 如果您有 CUDA 支援的 GPU，建議設為 'cuda' 以獲得更好的性能。
WHISPER_DEVICE=cpu

# 計算類型：int8 (CPU 優化), float16 (GPU 優化)
# 對於 CPU，'int8' 通常提供最佳性能。對於 GPU，'float16' 性能更優。
WHISPER_COMPUTE_TYPE=int8

# Qdrant Vector Database Configuration
# 這是 Docker 網路中 Qdrant 服務的名稱和端口
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334 # Qdrant gRPC 端口

# 日誌級別 (Python logging module)
LOG_LEVEL=INFO # 可選：DEBUG, INFO, WARNING, ERROR, CRITICAL

